"""Infrastructure layer: driven adapters (persistence, config, ...).

Implements the ports declared in ``ehson_bot.domain``. Depends on domain
and application; nothing in domain/application imports from here.
"""
